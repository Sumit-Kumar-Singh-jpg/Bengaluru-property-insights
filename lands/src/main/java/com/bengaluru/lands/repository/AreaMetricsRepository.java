package com.bengaluru.lands.repository;

import com.bengaluru.lands.entity.AreaMetrics;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AreaMetricsRepository extends JpaRepository<AreaMetrics, Integer> {
}